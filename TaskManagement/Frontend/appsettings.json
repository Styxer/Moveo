
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Configure the base URL for the backend API (YARP proxy address)
  "ApiBaseUrl": "http://localhost:5000/" 
}
